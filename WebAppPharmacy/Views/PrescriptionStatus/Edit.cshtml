@model WebAppPharmacy.Models.Dictionaries.PrescriptionStatus

@{
    ViewData["Title"] = "Редактирование статуса рецепта";
}

<div class="container mt-4">
    <h2 class="mb-4 text-warning">
        <i class="bi bi-pencil-fill me-2"></i> Редактирование статуса рецепта
    </h2>

    <div class="row">
        <div class="col-md-6">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="Id" />

                <div class="form-group mb-3">
                    <label asp-for="StatusName" class="form-label"></label>
                    <input asp-for="StatusName" class="form-control" />
                    <span asp-validation-for="StatusName" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <button type="submit" class="btn btn-primary me-2">
                        <i class="bi bi-check-circle-fill"></i> Сохранить
                    </button>
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Назад к списку
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.min.js"></script>
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}